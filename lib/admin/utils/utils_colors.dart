import 'package:flutter/material.dart';

class CustomColors {
  static const appTheme = Color.fromARGB(255, 25, 143, 198);
  static const List<Color> gradient = [
    Colors.red,
    Colors.black,
    Color.fromARGB(255, 0, 0,
        139), // Dark blue (modify alpha value for transparency if needed)
  ];
}
