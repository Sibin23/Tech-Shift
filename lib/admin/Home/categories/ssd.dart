import 'package:flutter/material.dart';

class ListSsd extends StatelessWidget {
  const ListSsd({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: SafeArea(child: Center(child: Text('List SSD'),)),
    );
  }
}