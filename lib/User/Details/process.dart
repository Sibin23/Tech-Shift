import 'package:flutter/material.dart';

class ScreenProcess extends StatelessWidget {
  const ScreenProcess({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('process'),),
    );
  }
}